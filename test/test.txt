method AREA_QUADRATO (int x, int y) {
    int z = x * y;
    return z
};

method Start (int x, int y) {
    int h=6;
    int l=5;
    int res = AREA_QUADRATO(l, h);

    print: "the base of the rectangle is ", l, " and its height is  ", h , "  and its area is ", res;
    return res
}








